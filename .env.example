# Local default uses SQLite (reservations.db). For Postgres on cloud, set DATABASE_URL.
# Example (Render/Heroku style):
# DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DBNAME
# SECRET_KEY=your-random-secret
