services:
  - type: web
    name: family-studyroom
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -c gunicorn.conf.py app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: studyroom-db
          property: connectionString
    healthCheckPath: /healthz
    autoDeploy: true
databases:
  - name: studyroom-db
    plan: free
